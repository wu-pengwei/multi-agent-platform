# 面试官视角：针对本项目的高概率问题题库

> 用法：本文模拟真实面试官，按"开场 → 四大模块深挖 → 底层原理 → 压力测试 → 开放延展"组织。
> 🔥 = 大概率被问 / 高危；⚡ = 考察深度的硬问题；每题附【考察点】，先自己答一遍再看建议。
> 所有问题都基于项目真实代码出题——面试官如果读过代码或追问细节，这些就是他会问的。

---

## A. 开场与整体（几乎必问）

**A1. 🔥 用两分钟介绍这个项目：解决什么问题、整体架构、你负责什么。**
【考察点】表达结构化能力。按"总问题 → 四模块各一句话 → 我的分工"来讲（见《面试项目介绍.md》口述稿）。

**A2. 🔥 这个项目是你一个人做的还是团队做的？你具体写了哪些部分？**
【考察点】贡献真实性。必须提前想清楚一个自洽的分工故事，能具体到模块和文件级别。

**A3. 项目里技术挑战最大的是哪个模块？为什么？**
【考察点】是否真做过。挑一个你最能讲的（建议：多 Agent 结果回传的中途注入，或记忆的两阶段设计），按"现象→排查→方案→效果"讲。

**A4. 如果重新做一遍，你会改哪些设计？**
【考察点】反思能力。准备好 2~3 条真实取舍（比如：子 Agent 只支持一层派生；记忆召回目前靠单一向量检索、可加混合检索）。

**A5. 这个项目的用户是谁？怎么衡量做得成功？**
【考察点】产品思维。个人助理/开发者场景 + 可量化指标（任务完成率、召回率、响应延迟）。

---

## B. 多智能体协作（核心深挖区）

**B1. 🔥 你的多 Agent 架构是什么形式？为什么不用群聊式（AutoGen 那种）或静态编排图（LangGraph 那种）？**
【考察点】选型能力。答法见《四模块痛点》文档：主从动态派生 + 总线回传；对比四个竞品形式的优劣。

**B2. ⚡ 主 Agent 怎么决定一个任务要不要拆、拆成几个？拆错了怎么办？**
【考察点】拆解决策机制。代码事实：拆解是 LLM 根据 SpawnTool 的描述自主决策的（何时该派生的规则写在工具描述里：复杂、耗时、可独立运行的任务）。拆错的兜底：主 Agent 收到结果后可以再派生纠正——即"结果反馈再规划"。

**B3. ⚡ 子 Agent 的结果怎么回传给主 Agent？主 Agent 正在处理别的消息时怎么办？**
【考察点】这是你项目里最深的机制，答好了是最大亮点。代码事实（`subagent.py:243-261`、`loop.py:249-252`）：结果以 `InboundMessage(channel="system", sender_id="subagent")` 发布回统一消息总线；主 Agent 空闲 → 触发新一轮处理；主 Agent 忙 → 路由到该会话的 pending queue 做**中途注入**（merge 进当前轮次，每轮有注入条数上限）。且结果在 prompt 组装前先持久化 + 按 task_id 去重，崩溃不丢、重放不重复。

**B4. 🔥 子 Agent 之间怎么通信？两个子 Agent 结论冲突怎么办？**
【考察点】是否真理解自己的架构。诚实答：子 Agent 之间**不直接通信**（刻意设计——工具集里没有 message tool），一切经主 Agent 汇总仲裁。冲突由主 Agent 判断，必要时派生第三个验证任务。

**B5. 🔥 为什么子 Agent 不能再派生子 Agent？**
【考察点】设计边界意识。代码事实（`subagent.py:156`）：子 Agent 的工具注册表里**刻意不给 SpawnTool 和 MessageTool**。理由：递归派生导致 Agent 树失控、token 成本不可控、错误难归因；单层结构 + 15 轮迭代上限保证系统有界。

**B6. ⚡ 子 Agent 的上下文里有什么？为什么不把主对话历史传给它？**
【考察点】上下文管理理解。代码事实：只有 `[system: 专用子Agent prompt, user: 任务描述]`，主对话历史不进入——上下文防火墙，防止污染；回传的也只有蒸馏后的结果模板。

**B7. ⚡ 子 Agent 执行到一半失败了怎么办？**
【考察点】容错设计。代码事实：`fail_on_tool_error=True`，失败时报告"已完成的步骤 + 失败点"（`_format_partial_progress`），主 Agent 可基于部分进度决定重试哪一段，而非整体重来。

**B8. 效率提升 15% 怎么测的？**
【考察点】数字可信度。口径：同一批多步骤复杂任务，单 Agent 串行 vs 多 Agent 并行，对比端到端完成时间（wall-clock）。见《四模块痛点》第六节。

**B9. 多个 Agent 同时写同一个文件，并发冲突怎么办？**
【考察点】并发意识。要能答出问题所在（无文件锁）和缓解手段（任务拆解时按目录/文件划分边界；文件工具有原子写保护）。

**B10. 子 Agent 的工具权限和主 Agent 一样吗？**
【考察点】最小权限意识。不一样：没有 spawn、没有 message（不能直接触达用户/渠道），文件操作限制在 workspace 内（sandbox 配置）。

**B11. ⚡ 主 Agent 是不是单点瓶颈？怎么改进？**
【考察点】承认 trade-off 的能力。是——协调和汇总都经主 Agent；但个人助理场景子任务大多可并行、弱依赖，匹配当前规模；规模大了可以引入专职"编排 Agent"。

---

## C. 长期记忆（核心深挖区）

**C1. 🔥 你的记忆系统怎么设计的？分几层？为什么分层？**
【考察点】架构能力。两层故事线要自洽：会话内短期记忆（上下文窗口 + 溢出压缩）+ 跨会话长期记忆（结构化文件 + 向量检索）。可以讲代码里的三层：session.messages（活对话）→ 压缩摘要归档 → 长期记忆文件（用户画像/事实决策），不同记忆用不同工具管理。

**C2. 🔥 什么信息值得写进长期记忆？怎么判断"重要性"？**
【考察点】记忆写入策略。答：用户约束/偏好（"花生过敏"）、稳定事实、任务决策；由 LLM 抽取判断重要性，触发时机是轮次阈值/会话结束。

**C3. 🔥 向量检索用什么实现？embedding 模型是什么？为什么选它？**
【考察点】技术真实性（说不出模型名会露馅）。代码事实：RAG 部分用 **chromadb 持久化存储 + SentenceTransformer all-MiniLM-L6-v2**（384 维，轻量本地推理，不依赖外部 API）；语义记忆层支持注入任意 embed 函数。答不出模型名是高危信号——务必记住。

**C4. ⚡ 检索回来的记忆怎么注入 prompt？注入几条？多了怎么办？**
【考察点】工程细节。top-k 限制注入量、按相关性排序、和当前上下文合并组装；记忆本身也要防挤爆窗口。

**C5. ⚡ 记忆冲突怎么办？用户先说"我喜欢简洁风格"，后来说"回答详细一点"？**
【考察点】记忆更新策略。答：新记忆写入时与旧记忆做冲突检测，过时信息标记失效而非物理删除（保留可追溯）；时效性上近期优先。

**C6. ⚡ 记忆量大了检索性能怎么办？几十万条记忆还能实时召回吗？**
【考察点】规模意识。向量库本身支持 ANN 索引（chromadb 内置 HNSW）；再加分层过滤（先按类型/时间粗筛再向量检索）；热记忆和冷记忆分级。

**C7. 20 轮对话测试集怎么构造的？10% 的 baseline 是什么？**
【考察点】评测口径。构造 N 组 20+ 轮对话，末尾提依赖早期关键信息的问题，有/无记忆机制对比正确率。主动交代口径。

**C8. ⚡ 为什么不直接对全部历史对话做 RAG，要先做记忆抽取和结构化？**
【考察点】方案对比深度。三点：①原始对话有噪音（寒暄、试错），抽取后的记忆信噪比高；②结构化才能分类管理（用户画像 vs 事实 vs 任务状态）；③检索效率——检索结构化记忆比检索海量原始对话快且准。

**C9. 用户要求"忘掉我说过的某件事"，怎么实现？**
【考察点】被遗忘权/数据治理意识。答：长期记忆带来源追溯（哪轮对话写入的），支持定位删除；向量库同步删除对应条目。

**C10. 记忆会不会被 prompt 注入污染？恶意用户让 Agent "记住"一条攻击指令？**
【考察点】安全意识。加分答：记忆写入走重要性/安全性过滤，注入到 prompt 时做隔离标记，长期记忆变更可审计（版本化）。

---

## D. 工具调用（深挖区）

**D1. 🔥 工具注册机制怎么设计的？新增一个工具要写什么？**
【考察点】扩展性设计。答：实现 Tool 基类 + 声明式参数 Schema（`tool_parameters` 装饰器），注册进 ToolRegistry 即被 Agent 感知，核心循环零改动——开闭原则。代码事实：`spawn.py:13-19` 就是标准示例。

**D2. 🔥 模型生成非法参数（参数幻觉）怎么防？**
【考察点】容错。JSON Schema 校验，失败把错误信息回灌模型重新生成（自我修正循环）；重试上限防死循环。

**D3. ⚡ 工具返回结果太大，把上下文撑爆怎么办？**
【考察点】上下文预算管理。代码事实：有 `max_tool_result_chars` 截断配置，超限截断/摘要后回灌，需要细节时可分页读取。

**D4. ⚡ 工具多了模型选错工具怎么办？**
【考察点】规模化问题。分组暴露/检索式工具发现、描述优化（描述里写清"何时用/何时不用"）、控制单次可见工具数量。

**D5. Shell 工具的安全怎么保证？模型执行 `rm -rf` 怎么办？**
【考察点】安全底线。代码事实：ExecTool 有 sandbox 模式 + workspace 路径限制 + 环境变量白名单（allowed_env_keys）+ 超时控制。答出"沙箱、路径限制、最小权限"三件套。

**D6. 🔥 MCP 是什么？你怎么接的？和自研工具注册机制什么关系？**
【考察点】生态理解。MCP 是开放的工具/资源协议，接入后 MCP server 的工具和自研工具统一进 ToolRegistry，对 Agent 透明——即"自研工具走注册接口，外部工具走 MCP 适配，最终同一抽象"。

**D7. Function Calling 的底层原理是什么？工具真的是模型"执行"的吗？**
【考察点】原理深度（高频！）。关键认知：模型只输出**结构化的调用意图**（工具名+参数的 JSON），真正执行工具的是你的代码；执行结果作为 tool role 消息回灌，模型基于结果继续生成。模型从头到尾只做文本生成。

**D8. 支持并行工具调用吗？怎么实现的？**
【考察点】异步能力。模型一次输出多个无依赖的 tool call，运行时并行执行（asyncio.gather）后统一回灌；有依赖的按序执行。

**D9. 工具描述的质量怎么影响调用准确率？怎么优化？**
【考察点】prompt 工程实操。描述要写清功能边界、参数含义、典型场景和反例；用 bad case 驱动迭代描述。

---

## E. 服务化部署（深挖区）

**E1. 🔥⚡ 高危：你简历写"基于 FastAPI"，但项目 API 服务实际用的是 aiohttp——为什么？**
【考察点】真实性 + 技术选型。这是最危险的一题，必须提前想好口径。两种可辩护的答法：
- 若你确实自己写过 FastAPI 层：明确说"核心 Agent 的 OpenAI 兼容服务是 aiohttp 实现的（选它是因为轻量、和异步消息总线同生态）；我在 XX 部分用 FastAPI 封装了 XX"。
- 若没有：把表述改准确（"基于异步 Web 框架封装"或直接学 FastAPI 的答法：原生 async、Pydantic 校验、SSE/WebSocket 支持等，并说明 aiohttp 的等价能力）。
关键：不要现场被问出框架名对不上——细节错一处，其他数字都会被怀疑。

**E2. 🔥 流式输出怎么实现的？**
【考察点】硬技能。代码事实：OpenAI 兼容接口支持 SSE（`text/event-stream`），token 级增量推送 + `[DONE]` 终止标记。要能讲清：模型流式响应 → 逐 delta 写入 HTTP 流 → 前端边收边渲染。

**E3. ⚡ 会话状态怎么管理？服务重启会话还在吗？多实例怎么办？**
【考察点】状态设计。会话按 session_key 路由，状态持久化到磁盘（重启可恢复，且有原子写+自动修复）；多实例要么按会话粘性路由，要么状态外置（Redis）。

**E4. 为什么做成 OpenAI 兼容的 API 格式？**
【考察点】生态意识。事实标准：所有工具链（SDK、客户端、评测框架）都默认支持 OpenAI 格式，兼容即零成本接入；也方便直接替换/对比模型服务。

**E5. ⚡ LLM 调用是 I/O 密集的，为什么 asyncio 合适？事件循环被阻塞会怎样？**
【考察点】并发原理。await 模型响应时事件循环可处理其他请求；要避免在 handler 里做同步重活（会卡整个循环）——CPU 密集的扔线程池/进程池。

**E6. API 鉴权怎么做？模型 API key 放哪？**
【考察点】安全。服务端集中管理凭证、不下发给调用方；对外接口做 token 鉴权、渠道白名单。

**E7. 怎么排查一个"Agent 回答变慢"的线上问题？**
【考察点】可观测性思维。分层定位：模型侧（首 token 延迟）→ 工具侧（哪个工具耗时，有 tool_events 记录）→ 队列侧（消息积压）→ 渠道侧。讲清"有埋点才能排查"。

---

## F. 底层原理（考深度，区分度最高）

**F1. ⚡ 完整讲一遍：一条用户消息进来，到回复出去，中间发生了什么？**
【考察点】系统级理解，几乎必考。标准答案链路：渠道收到消息 → 统一为 InboundMessage 进消息总线 → AgentLoop 取消息、定位会话 → 上下文组装（系统 prompt + 记忆注入 + 历史 + 自动压缩判断）→ LLM 推理 → 需要工具则执行工具、结果回灌、循环迭代 → 生成最终回复 → OutboundMessage → 渠道发回。

**F2. ⚡ 上下文窗口满了怎么办？**
【考察点】上下文管理。自动压缩（autocompact）：对话逼近上限时压缩最老的安全片段为摘要，活跃任务跳过压缩；跨会话靠长期记忆。

**F3. 消息总线为什么要设计成异步队列？直接函数调用不行吗？**
【考察点】解耦理解。渠道与核心解耦（加渠道不改核心）、天然背压（队列缓冲削峰）、支持多消费者/中途注入等高级路由。

**F4. 多渠道（微信/Telegram/飞书）怎么统一接入的？**
【考察点】抽象能力。渠道插件只做协议适配（各家 API、消息格式、媒体类型、鉴权），统一转成 InboundMessage/OutboundMessage 进总线；插件基类 + 注册表，新渠道零侵入。

**F5. 多家 LLM 提供商的 API 差异怎么抹平？**
【考察点】Adapter 模式落地。Provider 抽象基类统一接口（chat/stream/tool calling），差异（参数名、工具调用格式、思考模式）在各 Provider 内消化；注册表按名解析。

**F6. ⚡ Token 限制、限流、模型超时，分别怎么处理？**
【考察点】生产可靠性。上下文超限→自动压缩；限流→退避重试；超时→超时控制+失败回灌模型决策。

---

## G. 压力测试区（高危，提前备好，现场答不稳会连环崩）

**G1. 🔥🔥 项目是自研还是基于开源改的？改了哪些？**
【考察点】诚实度 + 实际参与度。必须提前准备自洽、具体到模块的回答。如果基于开源（本项目基于 nanobot），就明确说"基于 X 二次开发，我负责/重写了 A、B、C，解决了 D 问题"——二次开发本身不减分，讲不清细节才减分。

**G2. 🔥🔥 10% 和 15% 的测试集多大？ground truth 谁标的？**
【考察点】数字可信度。准备具体数字（如"50 组对话、每组末尾 1~2 个跨轮依赖问题，我人工标注判定标准"），并承认局限（规模有限、单人标注）——主动认局限比被戳穿强。

**G3. 🔥 你说的 "Workflow" 具体指什么？画一下调度流程。**
【考察点】概念是否落在实物上。要能白板画出：主 Agent → spawn 派生（并行）→ 子 Agent 执行（状态机）→ 总线回传 → 空闲触发/pending 注入 → 主 Agent 汇总。

**G4. 项目代码量多少？你写了多少行？**
【考察点】模糊问题，答相对值："核心 X 模块约 Y 行，其中我负责 Z"，不要报一个精确但可疑的总数。

**G5. 🔥 遇到过最难的 bug 是什么？怎么排查的？**
【考察点】真实开发经历的核心证据。必须准备一个具体的、有排查过程的故事（现象→假设→定位→修复→验证）。可以从这些真实机制里取材：子 Agent 结果路由错会话、消息重复注入、流式输出断连重连。

**G6. 为什么不用 Dify / Coze / LangGraph 这些现成平台？**
【考察点】自研的必要性。答：那些平台面向固定工作流场景，本项目要的是开放式个人 Agent（动态拆解、长期记忆、多渠道常驻）；且可控性/可定制性（记忆策略、渠道接入）是现成平台给不了的。

---

## H. 开放延展（答得好是印象分）

**H1. 如果要支持 1 万并发用户，架构要改哪里？**（会话状态外置 Redis、多实例+消息队列、模型网关限流、热数据缓存）

**H2. 多 Agent 之间会不会传播 prompt 注入？怎么防？**（子 Agent 输出视为不可信输入、回传内容隔离标记、工具权限收窄本身就是纵深防御）

**H3. 怎么看 Agent 的下一步演进？MCP 的意义是什么？**（工具生态标准化 = Agent 的"USB 接口"；记忆和身份协议可能是下一块）

**H4. 这个项目最后没做成/没做完的部分是什么？**（考察诚实度，讲 1~2 个真实遗憾 + 如果继续会怎么做）

---

## 备考优先级（时间有限就按这个顺序）

| 优先级 | 必须过关的题 |
|---|---|
| 第一梯队（答不上直接出局） | A1、A2、B1、C1、C3、D7、E1、E2、G1、G2、G5 |
| 第二梯队（答好是亮点） | B3、B5、C5、C8、D5、F1、F7、G3 |
| 第三梯队（余力再准备） | B9、C6、C9、C10、E7、F6、H 全部 |

**三个最需要提前想清楚答案的问题**（其他都可以临场发挥，这三个不行）：
1. **E1 框架名一致性**（FastAPI vs aiohttp）——口径不统一是硬伤；
2. **G1+G5 真实性故事**——分工故事和最难 bug 故事，必须具体到模块和排查过程；
3. **G2 数字口径**——两个百分比的方法论要能 30 秒讲清。
