# 项目技术全景：技术栈 / 分层架构 / 实现逻辑 / 坑点地图

> 用途：面试技术深挖环节的底层弹药库。所有内容基于仓库代码整理，文件路径可点击溯源。
> 配套：《面试项目介绍.md》（背景）、《四模块痛点》（痛点）、《面试题库-面试官视角》（问题）。

---

## 一、技术栈全景（按用途分组，均来自 `pyproject.toml` 与代码）

| 类别 | 选型 | 用在哪 |
|---|---|---|
| **语言/运行时** | Python ≥3.11（大量用 `asyncio`、`dataclass(slots=True)`、`X \| None` 新式类型） | 全部后端 |
| **LLM SDK** | `openai` + `anthropic` 官方 SDK（曾用 litellm，后移除，减少黑盒依赖） | `providers/` 多厂商适配 |
| **异步 HTTP/WS** | `httpx`（工具层请求）、`aiohttp`（API 服务）、`websockets`/`websocket-client` | 渠道、API、WebUI 通道 |
| **接口框架** | aiohttp Web（OpenAI 兼容 API + SSE 流式） | `api/server.py` |
| **CLI 框架** | `typer` + `rich` + `prompt-toolkit` + `questionary`（交互式配置向导） | `cli/` |
| **数据校验** | `pydantic` + `pydantic-settings` | 配置系统 `config/schema.py` |
| **工具协议** | `mcp` ≥1.26（Model Context Protocol 官方 SDK，含 SSE 传输） | `agent/tools/mcp.py` |
| **RAG/向量** | `chromadb`（持久化向量库）+ `sentence-transformers`（all-MiniLM-L6-v2，384 维） | `rag/vector_store.py` |
| **语义记忆** | 自研 JSONL 向量索引，`embed_func` 可注入任意嵌入函数 | `agent/semantic_memory.py` |
| **Token 计数** | `tiktoken` | 记忆压缩触发阈值判断 |
| **JSON 容错** | `json-repair`（修复模型输出的不合法 JSON） | 工具参数解析 |
| **模板** | `jinja2`（系统 prompt / 子 Agent 回传模板） | `utils/prompt_templates.py` |
| **记忆版本化** | `dulwich`（纯 Python 实现的 Git，操作长期记忆文件的版本历史） | `utils/gitstore.py` |
| **定时** | `croniter`（自然语言→cron 表达式→调度） | `cron/service.py` |
| **文档解析** | `pypdf`/`pymupdf`、`python-docx`、`openpyxl`、`python-pptx` | Office/PDF 阅读 |
| **网页正文** | `readability-lxml` + `chardet`（编码探测） | WebFetch 工具 |
| **搜索** | `ddgs` + 多引擎适配 | WebSearch 工具 |
| **渠道 SDK** | `python-telegram-bot`、`slack-sdk`、`lark-oapi`(飞书)、`dingtalk-stream`、`qq-botpy`、`discord.py`、`matrix-nio`、`PyJWT`(Teams) 等 | `channels/` 15 个渠道 |
| **日志** | `loguru` | 全局 |
| **观测** | `langsmith`/`langfuse`（可选） | LLM 调用追踪 |
| **存储后端** | 文件（默认）+ MongoDB（可选 `motor`/`pymongo`） | `agent/storage/` |
| **并发控制** | `filelock`（跨进程文件锁） | 工具执行 |
| **测试/工程** | `pytest` + `pytest-asyncio`（`asyncio_mode=auto`）+ `ruff` + `hatchling` 构建 | `tests/` |
| **前端** | WebUI：bun + vite（开发态，WebSocket 多路复用） | `webui/` |
| **部署** | Docker Compose：gateway/api/cli 三服务、`cap_drop: ALL` 最小权限、CPU/内存限额、重启策略 | `docker-compose.yml` |

**面试一句话概括技术选型**："整体是 Python asyncio 单进程多任务模型 + 官方 LLM SDK 直连 + 文件优先的持久化（JSONL/Markdown，可选 MongoDB），重的只有向量库（chromadb），其余全部轻量化——和'超轻量内核'的架构理念一致。"

---

## 二、七层架构（从下往上是依赖方向，从上往下是请求方向）

```
┌────────────────────────────────────────────────────────────────────┐
│ L7 服务/交付层   cli(typer) · api(aiohttp, OpenAI兼容+SSE) · WebUI │
│                 (bun/vite+WebSocket) · cron服务 · Docker编排        │
├────────────────────────────────────────────────────────────────────┤
│ L6 渠道接入层    15个渠道插件(telegram/wechat/feishu/discord/...)   │
│                 只做协议适配 → 统一 InboundMessage/OutboundMessage │
├────────────────────────────────────────────────────────────────────┤
│ L5 消息总线层    MessageBus = inbound/outbound 两条 asyncio.Queue  │
│                 （生产者：渠道/API/Cron/子Agent —— 四者同构）       │
├────────────────────────────────────────────────────────────────────┤
│ L4 调度层        AgentLoop：命令分流 · 会话路由 · 会话锁            │
│                 pending队列(中途注入) · 任务生命周期 · /stop恢复    │
├────────────────────────────────────────────────────────────────────┤
│ L3 推理层        AgentRunner(迭代循环+上下文治理+checkpoint)        │
│                 + Provider适配层(统一ToolCallRequest/LLMResponse)  │
├────────────────────────────────────────────────────────────────────┤
│ L2 能力层        ToolRegistry(文件/shell沙箱/搜索/RAG/MCP/cron)    │
│                 + Skills技能系统 + SpawnTool→SubagentManager       │
├────────────────────────────────────────────────────────────────────┤
│ L1 记忆/持久层   Session(JSONL原子写+损坏自修复) · MemoryStore     │
│                 (SOUL/USER/MEMORY.md + history.jsonl 游标)         │
│                 · SemanticMemory(向量) · GitStore(版本) · RAG       │
└────────────────────────────────────────────────────────────────────┘
```

**分层原则（面试重点讲这个）**：依赖只允许向下。L6 渠道不认识 L4 调度，L4 不认识具体 LLM 厂商，L2 工具不认识会话——所以"加渠道、换模型、加工具"都是各自层内的事，不动核心。这也是为什么子 Agent（L2 派生）回传结果要"降到" L5 总线重新进入链路，而不是直接调用 L4——**层级之间用消息解耦，不用函数调用**。

---

## 三、核心模块代码实现逻辑

### 3.1 调度层：AgentLoop（`agent/loop.py`）

```
run() 主消费循环:
  while running:
    msg = bus.consume_inbound()                 # 阻塞等待
    ├─ 优先级命令 → CommandRouter 内联分发，continue（不耗模型）
    ├─ effective_key = 统一会话键计算            # 支持跨渠道统一会话
    ├─ 该会话有活跃 pending 队列？
    │    ├─ 是 → 普通命令内联分发；否则 put 进 pending 队列（中途注入），continue
    │    └─ 否 → asyncio.create_task(_dispatch(msg))
    
_dispatch(msg):                                 # loop.py:623
  session_locks[key] 取/建锁                     # 会话内串行
  注册 pending 队列(maxsize=20)                  # 从此该会话新消息走注入
  _process_message(msg) → OutboundMessage
  finally: 残留 pending 消息重新发布回总线        # 不丢消息
```

关键实现细节：
- **同会话串行靠 `asyncio.Lock` 字典**（`_session_locks.setdefault`），跨会话天然并发——没有全局锁；
- **pending 队列注册在处理前**（[loop.py:631-634](../nanobot/agent/loop.py)），保证消息从派发到处理完成之间到达的后续消息都能被注入；
- **取消（/stop）不丢上下文**：工具执行期间持续写 checkpoint 到会话 metadata，取消时物化成历史（[loop.py:682-707](../nanobot/agent/loop.py)）。

### 3.2 推理层：AgentRunner（`agent/runner.py:229+`）

```
run(spec) 核心迭代（最多 max_iterations 轮）:
  每轮:
    1. 上下文治理（五连）:                      # runner.py:250-257
       _drop_orphan_tool_results     # 删孤儿工具结果
       _backfill_missing_tool_results # 补缺失工具结果
       _microcompact                 # 微压缩大消息
       _apply_tool_result_budget     # 工具结果预算截断
       _snip_history                 # 历史截断（截完再清一次孤儿）
    2. hook.before_iteration
    3. response = _request_model()   # Provider 调 LLM（支持流式回调）
    4. response.should_execute_tools？
       ├─ 是: 追加 assistant 消息 → 写 checkpoint → 执行工具
       │      → 工具结果逐条 append 为 tool 消息 → 致命错误则短路
       └─ 否: final_content 确定 → break
    5. 迭代边界 drain pending 队列   # 用户插话/子Agent结果注入，每轮有上限
```

**设计精髓**：上下文治理只修改"给模型看的副本"（`messages_for_model`），持久化的 `messages` 列表不动——**为模型做的手术不污染对话档案**（runner.py:246-249 注释明说了这个边界）。

### 3.3 Provider 适配层（`providers/base.py`）

统一数据结构抹平厂商差异：
- `ToolCallRequest`：各家工具调用统一结构，`to_openai_tool_call()` 序列化为 OpenAI 风格（存进会话的历史统一格式）；
- `LLMResponse`：统一响应，**专门为厂商怪癖留了字段**——`reasoning_content`（Kimi/DeepSeek-R1/MiMo 的思考内容）、`thinking_blocks`（Anthropic 扩展思考）、以及完整的错误元数据（`error_kind/error_code/retry_after`）驱动重试策略；
- `should_execute_tools` 属性（base.py:71-76）：**refusal / content_filter / error 状态下即使带工具调用也不执行**——防网关注入的假调用。

### 3.4 工具层（`agent/tools/`）

- **注册机制**：`Tool` 基类 + `@tool_parameters(schema)` 声明参数 Schema → `ToolRegistry.register()` 即对模型可见（开闭原则，见 [spawn.py:13-19](../nanobot/agent/tools/spawn.py) 的标准示例）；
- **SpawnTool → SubagentManager**（见《四模块痛点》文档详述）：`asyncio.create_task` 后台执行、独立 ToolRegistry（**无 spawn/message 工具**）、15 轮上限、状态机追踪（`SubagentStatus.phase`）、结果经总线回传；
- **ExecTool 沙箱**：sandbox 开关 + workspace 路径限制 + `allowed_env_keys` 环境变量白名单 + 超时；
- **MCP**：外部 MCP server 的工具/资源/prompt 统一适配进 ToolRegistry，与自研工具同构。

### 3.5 记忆层（`agent/memory.py` + `session/manager.py`）

**两阶段流水线 + 双存储**：

```
短期:  session.messages（内存+JSONL会话文件）
        │ AutoCompact.prepare_session 判断上下文压力
        ▼
阶段一: Consolidator.maybe_consolidate_by_tokens     # tiktoken 估 token
        最老安全片段 → LLM 摘要 → append 到 history.jsonl（游标推进）
        │
阶段二: Dream（cron 定时触发）
        读新增 history + 现有 SOUL.md/USER.md/MEMORY.md
        → LLM 两阶段: 先分析增量、再"外科手术式"最小化编辑长期文件
        → GitStore(dulwich) 提交版本，可回滚可审计
```

- **长期记忆四个文件**：`SOUL.md`（人格语气）/ `USER.md`（用户画像）/ `memory/MEMORY.md`（事实决策）/ `history.jsonl`（压缩归档，append-only + 双游标：`.cursor` 写 / `.dream_cursor` 读）；
- **语义检索**：`SemanticMemory`（JSONL + 向量，embed 函数注入）+ RAG 工具（chromadb）；
- **可选存储后端**：`MemoryStorageBackend` 抽象，默认文件、可切 MongoDB。

**会话持久化**（`session/manager.py`）：
- JSONL 逐行存（第一行 metadata，后续每行一条消息）；
- **原子写**：先写 `.jsonl.tmp` → `os.replace()` 原子替换（manager.py:282-312），关机时可 `fsync` 强刷；
- **损坏自修复**：`_repair()` 逐行解析，跳过坏行抢救剩余消息（manager.py:213）；
- `retain_recent_legal_suffix`：清历史时只保留"合法后缀"（不能从 tool 消息中间切断）。

### 3.6 服务层（`api/server.py` + Docker）

- **aiohttp** 实现 OpenAI 兼容接口：`/v1/chat/completions`、支持 `stream=true` 走 **SSE**（`text/event-stream`，逐 delta 推送 + `[DONE]` 结束）、文件上传、`session_key` 固定会话路由；
- **Docker Compose 三服务**：`gateway`(18790，WebSocket/WebUI) / `api`(127.0.0.1:8900) / `cli`(profile 可选)，`cap_drop: ALL` + 按需加回、CPU/内存限额、`restart: unless-stopped`。

---

## 四、整体链路（浓缩版，详版见上一轮分析）

```
用户 → 渠道插件(统一消息+语音转写) → 总线inbound
     → 主循环(命令分流→会话路由→忙则pending/闲则派task)
     → 预处理(崩溃恢复→AutoCompact→Consolidator→ContextBuilder组装)
     → AgentRunner迭代(上下文治理→LLM→工具执行回灌→边界注入)
     → _save_turn只追加新消息→原子落盘→后台再排Consolidator
     → 总线outbound → 渠道发回(流式逐段)
旁路: Cron/子Agent/API → 都以InboundMessage进总线 → 同一条链路
```

---

## 五、坑点地图（每条 = 真实现象 → 根因 → 代码里的防御）

> 这一节是面试"你踩过什么坑"的弹药库。以下坑全部来自这个代码库真实发生过并修复的问题（changelog 与代码注释可证）。

### A. 模型侧的坑

**A1. 厂商 API 响应格式不统一**
现象：换个模型工具调用解析就崩。根因：各家 tool_call 字段、思考内容（`reasoning_content` vs `thinking_blocks`）、finish_reason 语义都不同。防御：`ToolCallRequest`/`LLMResponse` 统一结构 + 厂商怪癖字段显式建模（base.py:48-63）。

**A2. refusal/content_filter 下带出的"假工具调用"**
现象：网关在拒绝状态下仍返回工具调用结构，直接执行等于执行了被审查拦截的内容。防御：`should_execute_tools` 显式校验 finish_reason（base.py:71-76，对应 issue #3220）。

**A3. 模型输出的工具参数 JSON 不合法**
现象：`json.loads` 直接抛异常。防御：引入 `json-repair` 库修复；这是行业普遍问题，能说出"我们专门引了一个修复库"就是实战痕迹。

**A4. 空回复 / 上下文超限**
现象：模型偶发返回空 content；超长直接报 length 错误。防御：`empty_content_retries` 空回复重试、`length_recovery_count` 超限恢复（压缩后重试）、`_microcompact` 微压缩（runner.py:239-241）。

### B. 上下文管理的坑

**B1. 孤儿工具结果（orphan tool results）**
现象：截断历史后 API 报 400——tool 消息必须紧跟带对应 tool_call 的 assistant 消息，截断切在中间就非法。防御：每轮迭代前五连治理，**截断后再清一次孤儿**（runner.py:250-257）；会话层 `retain_recent_legal_suffix` 只保留合法后缀（manager.py:87）。

**B2. 自动压缩误伤正在运行的任务**
现象：压缩把活跃任务的关键上下文压缩掉，任务行为突变。防御：auto-compact 跳过有活跃任务的会话（2026-04-13 changelog）。

**B3. 给模型的修剪污染了持久化历史**
现象：上下文治理改了消息，保存时把修剪写进了会话档案，历史被"二次加工"。防御：治理只作用于 `messages_for_model` 副本，持久化列表只追加新消息（runner.py:246-249 注释）。

### C. 并发与异步的坑

**C1. 同会话并发导致回复乱序**
根因：两条消息并行处理，后发先至。防御：`_session_locks` 每会话一把锁——**会话内串行、跨会话并发**（loop.py:628）。

**C2. 流式输出的边界粘包/丢字**
现象：token 增量在段边界重复或丢失。防御：流 delta 在边界处合并（stream delta coalescing，2026-03-26 changelog）；多段回答用 `stream_segment` 递增的 `_stream_id` 区分。

**C3. Cron 重载竞态**
现象：改定时任务配置时正在触发的任务行为异常。防御：Cron 重载守卫（2026-03-03 changelog）。

**C4. 跨进程文件竞争**
防御：`filelock`（工具执行与多实例场景）。

### D. 持久化的坑

**D1. 会话文件写一半崩溃 → 损坏**
防御：tmp 文件 + `os.replace` 原子替换（manager.py:282-312）+ `_repair()` 逐行抢救 + 关机 `flush_all(fsync=True)`（2026-04-19 changelog："atomic session writes with auto-repair"）。

**D2. 崩溃丢失用户消息**
防御：**用户消息在 prompt 组装前就先落盘**，`pending_user_turn` 标记 + 恢复逻辑（2026-04-13 changelog："user messages persisted early"）。

**D3. 子 Agent 结果被上下文合并"吞掉"**
现象：子 Agent 回传内容进不了会话历史——因为 ContextBuilder 为兼容 Provider 会**合并相邻同角色消息**，回传内容"可见于 prompt 却没进 history"，下次对话消失。防御：**在 prompt 组装前先持久化**子 Agent 回传（loop.py:786-793 注释完整记录了这个 bug）+ 按 `subagent_task_id` 去重防重放。**这是全项目最值得讲的坑**——有完整的现象→根因→修复故事。

**D4. 渠道消息重复投递**
现象：WhatsApp 等平台 at-least-once 投递导致重复处理。防御：消息级去重（2026-02-26 changelog）。

### E. 多智能体的坑

**E1. 递归派生失控**：子 Agent 再 spawn 子 Agent → Agent 树指数膨胀。防御：子 Agent 工具集**不含 SpawnTool**（subagent.py:156）+ 15 轮迭代上限。

**E2. 子 Agent 结果路由错会话**：结果要回到"发起会话"而非当独立任务处理。防御：`session_key_override` 对齐主 Agent 有效会话键，确保进对的 pending 队列（subagent.py:243-248 注释）。

**E3. 子 Agent 直接触达用户绕过汇总**：防御：工具集不含 MessageTool，唯一出口是总线回传。

### F. 渠道/平台的坑

**F1. 邮件渠道自回复死循环**：bot 回复自己的邮件 → 无限循环。防御：email self-loop guard（2026-04-17 changelog）。
**F2. Windows 路径分隔符**：`Windows path guard`（2026-02-26 changelog）。
**F3. 各渠道富文本格式互不兼容**（Slack mrkdwn / 飞书 CardKit / Discord 拆长消息）：渠道层各自适配渲染。
**F4. 会话投毒（session poisoning）**：伪造的会话数据注入（2026-02-26 安全修复）。

---

## 六、面试话术：怎么把这份材料讲成 60 秒

> "技术上，整个项目是 **Python asyncio 单进程多任务**模型，从下往上七层：文件优先的持久层（JSONL 原子写 + Markdown 记忆 + Git 版本化 + chromadb 向量库）、工具与子 Agent 能力层、带上下文治理的推理层、会话级调度的总线层、15 渠道接入层，最上面是 CLI / OpenAI 兼容 API / WebUI 三种交付形态。层与层之间**只用消息解耦、不做函数直调**，所以子 Agent 的结果也是走总线回到主循环，不引入第二套架构。
>
> 印象最深的坑是子 Agent 结果丢失的问题：回传内容能进模型 prompt，却没进持久化历史，下次对话就'失忆'了。排查后发现根因是上下文组装时为了兼容多家 Provider 会合并相邻同角色消息，把这条回传'合并没'了。最终的修复是把子 Agent 回传的持久化**提前到 prompt 组装之前**，并按任务 ID 去重。类似的防御在这个项目里到处都是——比如工具结果消息截断时会产生'孤儿 tool 消息'导致 API 400，所以每轮推理前有五步上下文治理；会话文件用临时文件加原子替换防止写一半损坏。**这个项目教会我最多的就是：LLM 应用 80% 的工程量在防御模型和渠道的'不规矩'上。**"

最后那句话是这段的灵魂——把零散的坑点上升成方法论认知，面试官会记住你。

---

## 附：快速自检清单（面试前一晚过一遍）

- [ ] 能默画七层架构图（层名 + 每层一个代表模块）
- [ ] 能讲出 5 个坑的完整故事（推荐：D3 子Agent回传丢失、B1 孤儿工具结果、C1 会话锁、D1 原子写、E1 递归失控）
- [ ] 技术选型能说出"为什么"：aiohttp vs FastAPI 口径、litellm 移除原因、chromadb + MiniLM、dulwich 做记忆版本化
- [ ] 记住四个关键数字/名词：15 渠道、子 Agent 15 轮上限、pending 队列 maxsize=20、每轮注入条数上限（`_MAX_INJECTIONS_PER_TURN`）
